version: 2

models:
  - name: dim_customers
    description: "One row per unique customer."
    columns:
      - name: customer_id
        data_tests: [unique, not_null]
      - name: total_orders
        data_tests: [not_null]