semantic_models:
  - name: products
    description: "Product dimension with category"
    model: ref('dim_products')

    entities:
      - name: product
        type: primary
        expr: product_id

    dimensions:
      - name: product_category_name_english
        type: categorical
        expr: product_category_name_english